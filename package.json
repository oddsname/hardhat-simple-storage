{
  "name": "hardhat-simple-storage",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "hardhat compile",
    "ganache:deploy": "hardhat run scripts/deploy.js --network ganache",
    "hardhat:deploy": "hardhat run scripts/deploy.js",
    "sepolia:deploy": "hardhat run scripts/deploy.js --network sepolia",
    "localhost:deploy": "hardhat run scripts/deploy.js --network localhost"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.2",
    "hardhat": "^2.14.0"
  },
  "dependencies": {
    "dotenv": "^16.0.3",
    "prettier": "^2.8.8",
    "prettier-plugin-solidity": "^1.1.3"
  }
}
